name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25.4"

      - name: Build all
        run: make build-all VERSION=${{ github.ref_name }}

      - name: Create GitHub Release and upload dist
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
          body: |
            ## Downloads
            Choose the binary that matches your OS and CPU architecture.

            | OS / Arch | Artifact |
            | --- | --- |
            | macOS / arm64 (Apple Silicon) | [gh-pr-formatter_mac_arm64](https://github.com/4okimi7uki/gh-pr-formatter/releases/download/${{ github.ref_name }}/gh-pr-formatter_mac_arm64) |
            | macOS / amd64 (Intel) | [gh-pr-formatter_mac_amd64](https://github.com/4okimi7uki/gh-pr-formatter/releases/download/${{ github.ref_name }}/gh-pr-formatter_mac_amd64) |
            | Linux / amd64 | [gh-pr-formatter_linux_amd64](https://github.com/4okimi7uki/gh-pr-formatter/releases/download/${{ github.ref_name }}/gh-pr-formatter_linux_amd64) |
            | Windows / amd64 | [gh-pr-formatter.exe](https://github.com/4okimi7uki/gh-pr-formatter/releases/download/${{ github.ref_name }}/gh-pr-formatter.exe) |
